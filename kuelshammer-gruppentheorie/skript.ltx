% Einige zusätzliche Informationen für rubber
%  rubber erkennt nicht, dass die Datei weg kann, daher sagen wir es ihm
% rubber: clean $base.thm
%  rubber soll nach Änderungen an der Datei nochmal bauen
% rubber: watch $base.thm
% rubber: index.tool      xindy
% rubber: index.language  german-din
%
% scrreprt trifft am Besten die Bedürfnisse eines Skripts, das ganze wird
% zweiseitig (twoside), d.h. es wird zwischen linker und rechter Seite
% unterschieden, und wir verwenden zwischen den Absätzen einen Abstand
% von einer halben Zeile (halfparskip) und dafür keinen Absatzeinzug,
% wobei die letzte Zeile eines Absatzes zu min. 1/4 leer ist.

\RequirePackage[l2tabu,orthodox]{nag}  % nag überprüft den Text auf veraltete
                   % Befehle oder solche, die man nicht in LaTeX verwenden
                   % soll -- l2tabu-Checker in LaTeX

%\RequirePackage[ngerman=ngerman-x-latest]{hyphsubst} % einbinden der neuen
                   % Trennmuster, diese korrigieren einige Fehler der alten
                   % und bieten mehr Trennstellen

\documentclass[ngerman,draft,parskip=half*,twoside]{scrreprt}

\usepackage{ifthen}
\usepackage{index}
\usepackage{xcolor}
\usepackage[draft=false,colorlinks,bookmarksnumbered,linkcolor=blue,breaklinks]{hyperref}

\usepackage[utf8]{inputenc}
\usepackage{babel}
\usepackage{lmodern}		% Latin Modern
% \usepackage{type1ec}           % cm-super
\usepackage[T1]{fontenc}        % T1-Schriften notwendig für PDFs
\usepackage{textcomp}           % wird benötigt, damit der \textbullet
                                % für itemize in lmodern gefunden wird.

\usepackage[intlimits,leqno]{amsmath}
\usepackage[all,warning]{onlyamsmath}  % warnt bei Verwendung von nicht
                                       % amsmath-Umgebungen z.\,B. $$...$$
\usepackage{amssymb}     % wird für \R, \C,... gebraucht
\usepackage{fixmath}     % ISO-konforme griech. Buchstaben
\usepackage[euro]{isonums} % definiert Komma als Dezimaltrennzeichen

\usepackage[amsmath,thmmarks,hyperref]{ntheorem} % für die Theorem-Umgebungen
                                                 % (satz, defini, bemerk)
\usepackage{xspace}      % wird weiter unten gebraucht
\usepackage{paralist}    % besseres enumerate und itemize und neue
                         % compactenum/compactitem; s. texdoc paralist

\usepackage{svn}         % Zum Auswerten und ordentlichen Darstellen der
                         % SVN-Schlüsselwörter (s. vor \begin{document})
                         % dafür muss in SVN noch das Flag svn:keywords
                         % auf "LastChangedRevision LastChangedDate"
                         % gesetzt werden
\usepackage{ellipsis}    % Korrektur für \dots
\usepackage{fixltx2e}
\usepackage[final,babel]{microtype} % Verbesserung der Typographie
\usepackage{mathtools}   % Zur Definition von \abs und \norm
\usepackage{todonotes}   % definiert den Befehl \todo{} um sich leicht
                         % Markierungen für offene Aufgaben zu setzen; wird
                         % auch für \help (s.u.) verwendet
\usepackage{nicefrac}
\usepackage{tikz}
\usepackage{wasysym}


% Damit auch die Zeichen im Mathemode in Überschriften fett sind
% <news:lzfyyvx3pt.fsf@tfkp12.physik.uni-erlangen.de>
\addtokomafont{sectioning}{\boldmath}

% nach dem Theoremkopf wird ein Zeilenumbruch eingefügt, die Schrift des
% Körpers ist normal und der Kopf wird fett gesetzt
\theoremstyle{break}
\theoremnumbering{arabic}
\theorembodyfont{\normalfont}
\theoremheaderfont{\normalfont\bfseries}

% Das Ende von Umgebungen, für die kein Beweis erbracht wurde, soll mit einer
% leeren Box gekennzeichnet werden. Wenn jedoch ein Beweis erbracht wurde,
% soll kein Zeichen ausgegeben werden (die ausgefüllte Box vom proof wird
% verwendet); man beachte die spezielle Definition von \theoremheaderfont für
% die Umgebung proof
% \newboolean{hasproof}
% \theoremheaderfont{\global\hasprooffalse\normalfont\bfseries}
% \theoremsymbol{\ifthenelse{\boolean{hasproof}}{}{\ensuremath{_\Box}}}

% Die folgenden Umgebungen werden einzeln nummeriert und am Ende jedes
% Kapitels zurückgesetzt
\newtheorem{satz}{Satz}[chapter]
\newtheorem{bemerk}{Bemerkung}[chapter]
\newtheorem{defini}{Definition}[chapter]
\newtheorem{bsp}{Beispiel}[chapter]
\newtheorem{festl}{Festlegung}[chapter]

% Die folgenden Theoremumgebungen bekommen keine Nummer
\theoremstyle{nonumberbreak}
\newtheorem{fakt}{Fakt}

% \theoremheaderfont{\global\hasprooftrue\scshape}
\theoremheaderfont{\scshape}
\theorembodyfont{\normalfont}
% Das Zeichen am Ende eines Beweises
\theoremsymbol{\ensuremath{_\blacksquare}}
% \theoremsymbol{q.\,e.\,d.}
\newtheorem{proof}{Beweis:}

% Hier die Definition, wie \autoref die Umgebungen nennen soll, die mit
% \newtheorem definiert wurden
\newcommand*{\satzautorefname}{Satz}
\newcommand*{\bemerkautorefname}{Bemerkung}
\newcommand*{\definiautorefname}{Definition}
\newcommand*{\bspautorefname}{Beispiel}
\newcommand*{\festlautorefname}{Festlegung}
% Zwischen Unter- und Unterunterabschnitten sollte nicht unterschieden
% werden.
\renewcommand*{\subsectionautorefname}{Abschnitt}
\renewcommand*{\subsubsectionautorefname}{Abschnitt}

\pagestyle{headings}

\newcommand*{\R}{\mathbb{R}}      % reelle Zahlen
\newcommand*{\C}{\mathbb{C}}      % komplexe Zahlen
\newcommand*{\N}{\mathbb{N}}      % natürliche Zahlen
\newcommand*{\Q}{\mathbb{Q}}      % gebrochene Zahlen
\newcommand*{\Z}{\mathbb{Z}}      % ganze Zahlen
\newcommand*{\F}{\mathbb{F}}      % Galois-Körper

% Wenn irgendwo Unklarheiten zum Inhalt im Skript auftreten, können sie
% einfach mit \help{Ich verstehe das nicht} hervorgehoben werden. Dies
% macht es leichter sie alle zu finden und auch ganz einfach
% auszublenden, indem man den Befehl einfach leer definiert
\newcommand*{\help}[1]{\todo[color=green!40]{#1}}

% Um wichtige Begriffe im Text überall gleich vorzuheben (gleiches
% Markup), sollte dieser Befehl verwendet werden. Das Argument wird
% automatisch als Indexeintrag verwendet. Dieser kann aber auch als
% optionales Argument selbst bestimmt werden.
\newcommand*{\highl}[2][]{\textbf{\boldmath{#2}}%
  \ifthenelse{\equal{#1}{}}{\index{#2}}{\index{#1}}%
}

% Befehl für die Darstellung der Gliederungsüberschriften im Index
\newcommand*{\lettergroup}[1]{\minisec{#1}}

% Für Leute, die nicht gern o.\,B.\,d.\,A. jedesmal eintippen wollen
\newcommand*{\obda}{o.\,B.\,d.\,A.\xspace}

% Um sicherzustellen, dass jeder Betrag/jede Norm links und rechts die
% Striche bekommt, sind diese Befehle da. Damit kann man nicht die
% rechten Striche vergessen und es wird etwas übersichtlicher. Aus
% mathtools.pdf, z. B. \abs[\big]{\abs{a}-\abs{b}} \leq \abs{a+b}
\DeclarePairedDelimiter{\abs}{\lvert}{\rvert}
\DeclarePairedDelimiter{\norm}{\lVert}{\rVert}

% Das original Epsilon sieht nicht so toll aus
\renewcommand*{\epsilon}{\varepsilon}
% ... und mancheinem gefällt auch das Phi nicht
\renewcommand*{\phi}{\varphi}


% kalligrafische Buchstaben
\newcommand*{\CO}{\mathcal{O}}


% Frakturbuchstaben
\newcommand*{\FP}{\mathfrak{P}}


% math. Operatoren
\DeclareMathOperator{\GL}{GL}
\DeclareMathOperator{\SL}{SL}
\DeclareMathOperator{\Sym}{Sym}
\DeclareMathOperator{\Alt}{Alt}
\DeclareMathOperator{\Aut}{Aut}
\DeclareMathOperator{\AO}{AO}
\DeclareMathOperator{\id}{id}
\DeclareMathOperator{\Abb}{Abb}
\DeclareMathOperator{\ggt}{ggT}
\DeclareMathOperator{\Hom}{Hom}
\DeclareMathOperator{\End}{End}
\DeclareMathOperator{\spn}{span}


\makeindex

\SVN $LastChangedRevision$
\SVN $LastChangedDate$

\begin{document}

\title{Gruppentheorie}
\author{Prof.\,Dr.\,Burkhard Külshammer}
\date{Semester: SS 2009}
\maketitle

\clearpage
\chapter*{Vorwort}

{\itshape
  Dieses Dokument wurde als Skript für die auf der
  Titelseite genannte Vorlesung erstellt und wird jetzt im Rahmen des
  Projekts
  "`\href{http://uni-skripte.lug-jena.de/}
  {Vorlesungsskripte der Fakultät für Mathematik}
  \href{http://uni-skripte.lug-jena.de/}{und Informatik}"'
  weiter betreut. Das
  Dokument wurde nach bestem Wissen und Gewissen angefertigt. Dennoch
  garantiert weder der auf der Titelseite genannte Dozent, die Personen,
  die an dem Dokument mitgewirkt haben, noch die
  Mitglieder des Projekts für dessen Fehlerfreiheit. Für etwaige Fehler
  und dessen Folgen wird von keiner der genannten Personen eine Haftung
  übernommen. Es steht jeder Person frei, dieses Dokument zu lesen, zu
  verändern oder auf anderen Medien verfügbar zu machen, solange ein
  Verweis auf die Internetadresse des Projekts
  \url{http://uni-skripte.lug-jena.de/}
  enthalten ist.

  Diese Ausgabe trägt die Versionsnummer~\SVNLastChangedRevision{} und ist vom
  \SVNDate{}. Eine neue Ausgabe könnte auf der Webseite des Projekts verfügbar
  sein.

  Jeder ist dazu aufgerufen, Verbesserungen, Erweiterungen und
  Fehlerkorrekturen für das Skript einzureichen bzw. zu melden oder diese
  selbst einzupflegen -- einfach eine E-Mail an die
  \href{mailto:uni-skripte@lug-jena.de}{Mailingliste
  \nolinkurl{<uni-skripte@lug-jena.de>}} senden. Weitere Informationen
  sind unter der oben genannten Internetadresse verfügbar.

  Hiermit möchten wir allen Personen, die an diesem Skript mitgewirkt
  haben, vielmals danken:
  \begin{itemize}
   \item \href{mailto:jens@kubieziel.de}{Jens Kubieziel
	       \nolinkurl{<jens@kubieziel.de>}} (2009)
  \end{itemize}
}

\clearpage
\pdfbookmark[0]{Inhaltsverzeichnis}{inhaltsverzeichnis}
\tableofcontents

\clearpage
\pdfbookmark[0]{Auflistung der Sätze}{theoremlist}
\chapter*{Auflistung der Theoreme}

\pdfbookmark[1]{Sätze}{satzlist}
\section*{Sätze}
\theoremlisttype{optname}
\listtheorems{satz}

\pdfbookmark[1]{Definitionen und Festlegungen}{definilist}
\section*{Definitionen und Festlegungen}
% \theoremlisttype{all}
\listtheorems{defini,festl}

\chapter{Einführung}



\section{Zahlbereiche}

\begin{enumerate}
 \item $(\Z,+), (\Q, +)$
 \item $(\Q\setminus{0}, \cdot)$
 \item $\Z/_{n\Z}$ der Restklassenring bzw. die Restklassengruppe modulo $n$
  mit der Addition.
 \item $(\Z/_{n\Z})^{\times}={a+n\Z\colon\ggt(a,n)=1}$ prime Restklassengruppe
  modulo $n$ mit der Multiplikation
\end{enumerate}

\section{Lineare Algebra}

Sei $K$ ein Körper  und $V$ ein $K$"~Vektorraum. Dann haben wir:
\begin{enumerate}
 \item $\GL(n,K)={A\in K^{n\times n}\colon \abs{A}\neq0}$ mit der
  Matrixmultiplikation. Dies ist die allgemeine lineare Gruppe des Grades $n$
  über $K$.
 \item $\GL(V)={f\colon V\rightarrow V\colon f\text{ linear und bijektiv}}$.
 \item $\SL(n,K)={A\in K^{n\times n}\colon\abs{A}=1}$ die spezielle lineare
  Gruppe des Grades $n$ über $K$.
\end{enumerate}

Sei $V$ ein euklidischer Vektorraum mit dem Skalarprodukt
\begin{enumerate}
 \item $O(V)=\{f\colon V\rightarrow V\colon f\text{ Isometrie}\}$ ist die
  orthogonale Gruppe von $V$. Dabei bedeutet Isometrie, dass $f$ linear ist
  und für alle $x,y$ aus $V$ gilt: $\langle f(x), f(y)\rangle=\langle
  x,y\rangle$.
 \item $O(n)=O(n,\R)=\{A\in\R^{n\times n}\colon A^{T}A=1_{n}\}$ orthogonale
  Gruppe des Grades $n$.
\end{enumerate}

Sei $V$ ein unitärer Vektorraum mit dem Skalarprodukt
\begin{enumerate}
 \item $U(V)=\{f\colon V\rightarrow V\colon f\text{ Isometrie}\}$ unitäre
  Gruppe von $V$
 \item $U(n)=U(n,\C)=\{A\in\C^{n\times n}\colon\overline{A}^{T}A=1_{n}\}$
\end{enumerate}

\section{Kombinatorik}
Sei $\Omega$ eine Menge.
\begin{enumerate}
 \item $\Sym(\Omega)= \{f\colon\Omega\rightarrow\Omega\colon f\text{
  bijektiv}\}$ symmetrische Gruppe auf $\Omega$
 \item $\Alt(\Omega)= \{f\in\Sym(\Omega)\colon f\text{ gerade}\}$
  alternierende Gruppe auf $\Omega$. Dabei muss $\Omega$ endlich sein.
\end{enumerate}

\section{Geometrie}

\begin{enumerate}
 \item $\AO(\R^{n})=\{f\colon\R^{n}\rightarrow\R^{n}\colon\norm{f(x)-f(y)}=
  \norm{x-y} \forall x,y\in\R^{n}\}$ Bewegungsgruppe.
 \item Symmetriegruppe des
  regelmäßigen $n$"~Ecks $P_{n}$ mit $G=\{f\in\AO(\R^{2})\colon f(P_{n})=
  P_{n}\}$ \textsc{Dieder}gruppe. Die Gruppe hat $2n$~Elemente.\\
\begin{tikzpicture}[thick,
information text/.style={rounded corners,fill=red!10,inner sep=1ex}]
\draw (60:4ex) -- (120:4ex) -- (180:4ex) -- (240:4ex) -- (300:4ex)
-- (360:4ex) -- cycle;
\draw[xshift=1.85cm]
  node[right,text width=6cm,information text]
  {
    Bild der $P_6$-Gruppe
  };
\end{tikzpicture}
 \item Friesgruppen: Symmetriegruppen von Friesen.
 \item kristallografische Gruppen in der Ebene oder im Raum:\\
\begin{tikzpicture}
\foreach \x in {1,2,3,4,5}
\draw (\x,0) -- (\x,1);
\foreach \y in {.1,.3,.5,.7,.9}
\draw (.1,\y) -- (6,\y);
%\draw (0,0) grid [step=.5cm] (5,3);
\end{tikzpicture}
\begin{tikzpicture}[thick]
\foreach \x in {1,2,3,4,5}
\draw (\x,0) -- (\x+1,1);
\foreach \y in {.1,.3,.5,.7,.9}
\draw (1,\y) -- (6,\y);
\end{tikzpicture}
 \item Symmetriegruppen von Tetraeder, Würfel, Ikosaeder und ähnlichen Objekten.
\end{enumerate}

\section{Algebra}

Sei $L|K$ eine Körpererweiterung. Diese besitzt eine \textsc{Galois}gruppe
$\F(L|K)=\{f\colon L\rightarrow L\colon f \text{ Automorphismus von } L,
f|K=\id_{K}\}$.

\section{Topologie}
\begin{enumerate}
 \item Fundamentalgruppen, Homologiegruppen, \dots{}, 
\end{enumerate}

\section{Zahlentheorie}
Sei $K$ ein algebraischer Zahlkörper und $\CO_{K}$ der Ganzheitsring.
\begin{enumerate}
 \item Einheitengruppe: $\CO_{K}^{\times}= \{a\in \CO_{K}\setminus\{0\}\colon
  \nicefrac{1}{a}\in\CO_{K}\}$
 \item Klassengruppe
\end{enumerate}

\section{Beliebige mathematische Theorie}

Sei $M$ ein Objekt dieser Theorie. Dann hat man mindestens eine Gruppe, die
Automorphismengruppe $\Aut(M)$ von $M$. Beispiele sind Lie"~Algebren,
Codierungstheorie, \textsc{Banach}räume etc.


\chapter{Halbgruppen}

\begin{defini}[Monade, Magma]
  Sei $M$ eine Menge. Darauf legen wir eine \highl{Verknüpfung} fest $M\times
  M\rightarrow M$ mit $(a,b)\mapsto a\times b$ (alternativ auch $a+b, a\cdot
  b$ oder $ab$). Dann ist $(M,\times)$ eine \highl{Monade} oder auch
  \highl{Magma}.
\end{defini}

\begin{bsp}
  \begin{enumerate}[(i)]
   \item Die Addition, Subtraktion und Multiplikation auf den natürlichen,
    rellen und komplexen Zahlen.
   \item Der Durchschnitt oder die Vereinigung auf der Potenzmenge $\FP(X)$.
   \item Der größte gemeinsame Teiler oder das kleinste gemeinsame Vielfache
    auf $\N$.
   \item $\circ$ auf $\Abb(X)=\{f\colon X\rightarrow X\colon f\text{
    Abbildung}\}$
  \end{enumerate}
\end{bsp}

\begin{bemerk}
  Wenn $M$ klein ist, kann man eine Verknüpfungstafel aufstellen.  Ein
  Beispiel sind die Wahrheitswerte.
\begin{table}[htb]
\centering
\begin{tabular}{c|cc}
  $\wedge$ & $w$ & $f$\\
  \hline
  $w$ & $w$ & $f$\\
  $f$ & $f$ & $f$
\end{tabular}
\caption{Wahrheitswerte für die UND-Verknüpfung}
\end{table}
\end{bemerk}

\begin{defini}[rechts-, linksneutral, neutral] Sei $M$ eine Monade und $e\in
  M$. Das Element $e$ ist genau dann \highl{rechtsneutral} (oder
  \highl{linksneutral}), wenn für alle $a\in M$ gilt: $ae=a$ (oder $ea=a$). Man
  nennt $e$ genau dann neutral, wenn es rechts- und linksneutral ist.
\end{defini}

\begin{bemerk}
  Sei $e\in M$ linksneutral und $f\in M$ rechtsneutral. Dann folgt, $e=ef=f$.
  Insbesondere existiert in $M$ höchstens ein neutrales Element.
\end{bemerk}

\begin{bsp}
  Die $0$ ist neutral in $(\Z,+)$ und die $1$ ist neutral in $(\Z,\cdot)$.
\end{bsp}

\begin{defini}[Vertauschbarkeit, Kommutativität]
  Sei $M$ eine Monade und $a,b\in M$. Die Elemente $a$ und $b$ heißen
  \highl{vertauschbar}, wenn gilt: $ab=ba$. Die Menge $M$ heißt
  \highl{kommutativ} oder \highl{abelsch}, wenn alle Elemente vertauschbar sind.
\end{defini}

\begin{defini}[Halbgruppe, Monoid]
  Die Menge $M$ heißt genau dann \highl{Halbgruppe}, wenn $(xy)z= x(yz)$ und
  ein \highl{Monoid}, wenn $M$ eine Halbgruppe mit neutralem Element ist.
\end{defini}

\begin{bsp}
  \begin{enumerate}
   \item $(\N,+)$ Halbgruppe, $(\N_{0}, +)$ Monoid
   \item $X$ Menge $\Rightarrow\Abb(X)$ Monoid, neutrales Element
    $\id_{X}\colon X\rightarrow X, x\mapsto x$ identische Abbildung
   \item Sei $A\neq\emptyset$ eine Menge (\highl{Alphabet}). Die Elemente von
    $A$ heißen \highl{Buchstaben}. Ein \highl{Wort} über $A$ ist die endliche
    Folge $w=(a_{1},\dotsc, a_{m})=\colon a_{1}\cdot\dotso \cdot a_{m}$. Die
    \highl[Halbgruppe!freie]{freie Halbgruppe} über $A$ ist definiert als
    $W\coloneq\{w\colon w\text{ Wort über } A\}$. Das
    \highl[Wort!leeres]{leere Wort} ist $\epsilon=()\notin W$. Dann können wir
    das \highl[Monoid!freies]{freie Monoid} über $A$ mit $W_{0}\coloneq
    W\cup\{\epsilon\}$ definieren.
  \end{enumerate}
\end{bsp}

\begin{bemerk}
  Die neutralen Elemente werden oft mit $1$ bezeichnet. Falls $+$ die
  Verknüpfung bezeichnet, verwendet man auch $0$ als neutrales Element.
\end{bemerk}

%def 1.4
\begin{defini}[Invertierbarkeit]
  Sei $M$ ein Monoid und $a\in M$. Das Element $a$ heißt genau dann
  \highl{rechtsinvertierbar} (oder \highl{linksinvertierbar}), wenn ein
  Element $b\in M$ mit $ab=1$ ($ba=1$) existiert. Man bezeichnet $b$ als das
  \highl{Rechtsinverse} oder \highl{Linksinverse} zu $a$.
\end{defini}

\begin{bemerk}
  Sei $b\in M$ rechtsinvers und $c\in M$ linksinvers zu $a$. Dann folgt,
  $b=1b=(ca)b= c(ab)= c1=c$. Das Element $a$ heißt dann \highl{invertierbar}
  und $b$ \highl{Inverses} zu $a$. Wir schreiben $b=\colon a^{-1}$ oder bei
  der Addition $b=\colon-a$. Es gilt: $a^{-1}a=1=aa^{-1}$. Damit ist auch
  $a^{-1}$ invertierbar und wir haben $(a^{-1})^{-1}=a$. Wenn zwei Elemente
  $x,y\in M$ invertierbar sind, dann ist $xy$ invertierbar und $(xy)^{-1}=
  y^{-1}x^{-1}$. Denn $xyy^{-1}x^{-1}=x1x^{-1}=1$.
\end{bemerk}

\begin{bsp}
  Sei $K$ ein Körper und $n$ eine natürliche Zahl. Dann folgt für das Monoid
  bezüglich der Matrxmultiplikation $K^{n\times n}$ mit der Einheitsmatrix als
  natürliches Element. Eine Matrix $A\in K^{n\times n}$ ist invertierbar, wenn
  $A$ eine reguläre Matrix ist, d.\,h. die Matrix von $A$ ist ungleich $0$. In
  der linearen Algebra impliziert die Linksinvertierbarkeit auch die
  Rechtsinvertierbarkeit.
\end{bsp}

%def 1.5
\begin{defini}[Potenz]
  Sei $H$ eine Halbgruppe und $a\in H, n\in N$. Die $n$"~te \highl{Potenz} von $a$ ist
  definiert als $a^{n}\coloneq a\cdot\dotso \cdot a$ mit $n$~Faktoren. Wenn
  $H$ ein Monoid ist, gilt: $a^{0}\coloneq1$. Sollte $a$ invertierbar sein,
  dann können wir negative Potenzen festlegen: $a^{-n}= (a^{-1})^{n}$.
\end{defini}

\begin{bemerk}
  Rechenregel: $a^{n}a^{m}= a^{n+m}, (a^{n})^{m}=a^{nm}$, wenn $a,b$
  vertauschbar sind, gilt auch: $(ab)^{n}= a^{n}b^{n}$. Ist $+$ die
  Verknüpfung, so schreibt man: $na$ statt $a^{n}$. Dann sehen die
  Rechenregeln so aus: $(m+n)a= ma+na, m(na)= (mn)a$, wenn $a,b$ vertauschbar
  sind, so gilt noch: $n(a+b)= na+nb$.
\end{bemerk}

%def 1.6
\begin{defini}[Homomorphismus]
  Seien $M,N$ zwei Monaden und wir betrachten die Abbildung $f\colon
  M\rightarrow N$.
  \begin{enumerate}
   \item Die Abbildung $f$ heißt genau dann \highl{Homomorphimus}, wenn für
    alle $a,b\in M$ gilt: $f(ab)=f(a)f(b)$.
   \item $f$ Monomorphismus $\Leftrightarrow f$  injektiver Homomorphimus
   \item $f$ Epimorphismus $\Leftrightarrow f$ surjektiver Homomorphimus
   \item $f$ Isomorphismus $\Leftrightarrow f$ injektiver Homomorphimus
   \item $f$ Endomorphismus $\Leftrightarrow f$ Homomorphimus und $M=N$
   \item $f$ Automorphismus $\Leftrightarrow f$ bijektiver Endomorphismus
  \end{enumerate}
  Wir setzen $\Hom(M,N)\coloneq\{f\colon M\rightarrow N\colon f \text{
  Homomorphimus}\}, \End(M)\coloneq\Hom(M,M),
  \Aut(M)\coloneq\{f\in\End(M)\colon f\text{ bijektiv}\}$.
\end{defini}

%Vorlesung vom 2009-04-16
\begin{bsp}
  \begin{enumerate}
   \item Sei $K$ ein Körper und $n$ eine natürliche Zahl. Dann  ist
    $\det\colon(K^{n\times n},\cdot)\rightarrow(K,\cdot)$ ein Homomorphimus.
   \item Die Exponentialfunktion von $(\R,+)$ auf $(\R,\cdot)$.
   \item Sei $W$ die freie Halbgruppe über einem Alphabet $A$ und $w\in W$ mit
    $w=a_{1},\dotsc, a_{n}, a_{1},\dotsc, a_{n}\in A$. Die Funktion
    $l(w)\coloneq n$ ist die Länge des Wortes. Dann ist $l\colon
    W\rightarrow(\N,+)$ ein Homomorphimus.
  \end{enumerate}
\end{bsp}

\begin{bemerk}
  \begin{enumerate}
   \item Seien $L,M,N$ Monaden und $f\in\Hom(L,M), g\in\Hom(M,N)$. Dann ist
    $g\circ f\in\Hom(L,N)$ ein Homomorphimus. Denn $(g\circ f)(ab)= g(f(ab))=
    g(f(a)f(b))= g(f(a))g(f(b))= (g\circ f)(a)\cdot(g\circ f)(b)$ für $a,b\in
    L$.
   \item Sei $f$ ein Isomorphismus. Dann ist $f^{-1}$ ebenfalls ein
    Isomorphismus. Denn $f^{-1}(xy)=f^{-1}(f(f^{-1}(x)) f(f^{-1}(y)))=
    f^{-1}(f(f^{-1}(x)f^{-1}(y)))= f^{-1}(x)f^{-1}(y)$.
  \end{enumerate}
\end{bemerk}

%def 1.7
\begin{defini}[isomorph]
  Die Monaden $M,N$ heißen \highl{isomorph} ($M\cong N$), falls ein Isomorphismus
  $f\colon M\rightarrow N$ existiert.
\end{defini}

\begin{bsp}
  Es gilt: $(\{w,f\},\vee)\cong(\{0,1\}, \cdot)$. Zum Nachweis kann man die
  Verknüpfungstafel prüfen.
\end{bsp}

\begin{satz}
  Die Isomorphie ist eine Äquivalenzrelation, d.\,h. es gilt:
  \begin{enumerate}
   \item $M\cong M$ (Reflexivität)
   \item $M\cong N\Rightarrow N\cong M$ (Symmetrie)
   \item $L\cong M\wedge M\cong N\Rightarrow L\cong N$ (Transitivität)
  \end{enumerate}
\end{satz}

\chapter{Gruppen}

%def 2.1
\begin{defini}[Gruppe]
  Eine \highl{Gruppe} ist eine Halbgruppe $G$ mit einem linksneutralen Element
  $e$, in der zu jedem Element $g\in G$ ein weiteres $h\in G$ mit $hg=e$
  existiert.
\end{defini}

\begin{satz}
  Eine Gruppe ist ein Monoid, in dem jedes Element invertierbar ist.
  \begin{proof}
    Seien $G,e,g,h$ wie oben. Zu dem Element $h$ existiert ein $k\in G$ mit
    $kh=e$. Dann ist $ke= khg=e g =g$ und weiter $ge=kee=ke=g$. Folglich ist
    $e$ neutral.
    Für den Nachweis der Rechtsinvertierbarkeit sei $g=ke=k$, d.\,h. $gh=e$.
    Somit ist $g$ invertierbar.
  \end{proof}
\end{satz}


\begin{bsp}
  \begin{enumerate}
   \item $(\Z,+), (\Q,+), (\R,+)$ sind abelsch. Dagegen ist $(\N,+)$
    \emph{keine} Gruppe.
   \item $(\Q\setminus\{0\}, \cdot), (\R\setminus\{0\}, \cdot),
    (\C\setminus\{0\}, \cdot)$. Aber $(\Z\setminus\{0\}, \cdot)$ ist
    \emph{keine} Grupe.
   \item Sei $M$ ein Monoid und $U(M)\coloneq\{a\in M\colon a\text{
    invertierbar}\}$. Dann heißt $U(M)$ \highl{Einheitengruppe} von $M$.
   \item Sei $X$ eine Menge und die Einheiten aller Abbildungen von $X$ in
    sich: $U(\Abb(X))=\{f\colon X\rightarrow X\colon f\text{ bijektiv}\}$.
    Dies wird als \highl[Gruppe!symmetrische]{symmetrische Gruppe} $\Sym(X)$
    auf $X$ bezeichnet. Die Elemente der Gruppe heißen \highl{Permutation}en
    auf $X$. Für $X=\{1,\dotsc,n\}$ schreibt man $\Sym(n)\coloneq\Sym(X)$. Die
    Elemente heißen dann Permutationen des Grades $n$. Schreibweise:
    \begin{gather*}
      f=
	 \begin{pmatrix}
	   1 & 2 & \dotso & n\\
	   f(1) & f(2) & \dotso & f(n)
	 \end{pmatrix}
    \end{gather*}
    Dann ist
    \begin{gather*}
      f=
	 \begin{pmatrix}
	   f(1) & f(2) & \dotso & f(n)
	   1 & 2 & \dotso & n\\
	 \end{pmatrix}
    \end{gather*}
   \item Sei $K$ ein Körper und $n\in\N$. Dann ist $U(K^{n\times n},
    \cdot)=\{A\in K^{n\times n}\colon\det A\neq0\}=\GL(n,K)$.
   \item Für jede nichtleere Familie $(G_{i})_{i\in I}$ von Gruppen $G_{i}$.
    Dann ist auch das \highl[Produkt!direktes]{direkte Produkt} $\prod_{i\in
    I} G_{i}= \times_{i\in I}G_{i}= \{(g_{i})_{i\in I}\colon g_{i}\in G_{i}
    \forall i\in I\}$ eine Gruppe\todo{richtiges grosses $X$ suchen.}
    mit $(g_{i})(h_{i})\coloneq(g_{i}h_{i})$. Im Fall $I=\{1,\dotsc,n\}$
    schreibt man $\prod_{i=1}^{n} G_{i} = \times_{i=1}^{n}= G_{1}\times\dotso
    \times G_{n}$.
  \end{enumerate}
\end{bsp}

%def 2.2
\begin{defini}[Ordnung]
  Die \highl{Ordnung} $\abs{G}$ einer Gruppe ist die Anzahl der Elemente.
\end{defini}

\begin{satz}
  \begin{enumerate}
   \item Sei $n\in N$. Dann ist $\abs{\Sym(n)}=n!$.
   \item Sei $K$ ein Körper und $\abs{K}=q<\infty$. Dann ist $\abs{\GL(n,K)}=
    (q^{n}-1)(q^{n}-q)\dotso (q^{n}-q^{n-1})$.
  \end{enumerate}
  \begin{proof}
    \begin{enumerate}
     \item Sei $f\in\Sym(n)\Rightarrow f(1)\in\{1,\dotsc,n\},
      f(2)\in\{1,\dotsc,n\}\setminus\{f(1)\},
      f(3)\in\{1,\dotsc,n\}\setminus\{f(1), f(2)\},\dotsc$.
     \item Sei $A=(a_{ij})\in\GL(n,K)\Rightarrow a_{1}\coloneq(a_{11},\dotsc,
      a_{1n})\in K^{n}\setminus\{0\}, a_{2}\coloneq(a_{21},\dotsc, a_{2n})\in
      K^{n}\setminus\spn(a_{1}), a_{3}\coloneq(a_{31},\dotsc, a_{3n})\in
      K^{n}\setminus\spn(a_{1},a_{2}),\dotsc$.
    \end{enumerate}
  \end{proof}
\end{satz}

%satz 2.3
\begin{satz}\todo{Satz mit Nummer}
  Sei $f\colon G\rightarrow H$ ein Gruppenhomomorphismus. Dann ist $f(1_{G})=
  1_{H}$ und $f(g^{-1})=f(g)^{-1}$ für $g\in G$.
  \begin{proof}
    Es gilt: $f(1_{G})= f(1_{G})1_{H}= f(1_{G})f(1_{G})f(1_{G})^{-1}=
    f(1_{G}\cdot1_{G})f(1_{G})^{-1}= 1_{H}$ und weiter: $f(g^{-1})=
    f(g^{-1})1_{H}= f(g^{-1})f(g)f(g)^{-1}= f(g^{-1}g)f(g)^{-1}= f(1_{G})
    f(g)^{-1}= 1_{H}f(g)^{-1}= f(g)^{-1}$.
  \end{proof}
\end{satz}

\begin{bsp}
  Sei $K$ ein Körper und $n$ eine natürliche Zahl. Dann ist
  $\det\colon\GL(n,K)\rightarrow K\setminus\{0\}$ ein Homomorphimus. Somit
  folgt, $\det(1_{n})=1$ und $\det(a^{-1})= \det(a)^{-1}$.
\end{bsp}

%def 2.4
\begin{defini}[Untergruppe]
  Eine Teilmenge $U$ einer Gruppe $G$ heißt \highl{Untergruppe} von $G$, wenn
  gilt:
  \begin{enumerate}
   \item $1_{G}\in U$
   \item $a,b\in U\Rightarrow ab,a^{-1}\in U$
  \end{enumerate}
\end{defini}

\begin{bemerk}
  Gegebenenfalls ist $U$ mit der entsprechend eingeschränkten Verknüpfung
  selbst eine Gruppe. Wir schreiben dann $U\leq G$ oder $U<G$, wenn $U\neq G$
  ist. Man bezeichnet $U$ dann als \highl[Untergruppe!echte]{echte Untergruppe}.
\end{bemerk}

\begin{bsp}
  \begin{enumerate}
   \item $(\Z,+)<(\Q,+)<(\R,+)<(\C,+)$.
   \item In jeder beliebigen Gruppe $G$ sind $G$ selbst und die
    \highl[Untergruppe!triviale]{triviale Untergruppe} $\{1_{G}\}=1$
    Untergruppen.
   \item Für jede nichtleere Familie $(G_{i})_{i\in I}$ von Gruppen bilden die
    Elemente $(g_{i})_{i\in I}$ aus dem direkten Produkt mit $\abs{\{i\in
    I\colon g_{i}\neq1\}}<\infty$ eine Untergruppe von $\prod_{i\in I} G_{i}$.
    Diese heißt \highl[Produkt!direktes eingeschränktes]{direktes
    eingeschränktes Produkt} von $(G_{i})_{i\in I}$. Bezeichnung: $\coprod_{i\in
    I} G_{i}$
    Für $\abs{I}<\infty$ ist $\coprod_{i\in I} G_{i}= \prod_{i\in I} G_{i}$
    \todo{drop}
   \item Für jede Monade $M$ ist die Automorphismengruppe $\Aut(M)$ eine
    Untergruppe von $\Sym(M)$.
  \end{enumerate}
\end{bsp}

\begin{satz}\label{satz:24}
  Eine nichtleere Teilmenge $U$ einer Gruppe $G$ ist genau dann eine
  Untergruppe von $G$, wenn gilt: $a,b\in U\Rightarrow ab^{-1}\in U$.
  \begin{proof}
    Wir brauchen nur die Rückrichtung zu zeigen. Die andere Richtung ist klar.
    Sei dazu $U\subseteq G$ nichtleer und die obige Bedingung sei erfüllt.
    Dann existiert ein $x\in U$. Folglich $1_{G}=xx^{-1}\in U$. Also
    $x^{-1}=1_{G}x^{-1}\in U$. Andererseits gilt für $y\in U$:
    $x(y^{-1})^{-1}\in U$.
  \end{proof}
\end{satz}

%def 2.5
\begin{defini}
  Für Teilmengen $X,Y$ einer Gruppe $G$ setzt man: $XY\coloneq\{xy\colon x\in
  X,y\in Y\}$ und $X^{-1}\coloneq\{x^{-1}\colon x\in X\}$.
\end{defini}

\begin{bemerk}
  Dann ist $(X^{-1})^{-1}=X, (XY)^{-1}= Y^{-1}X^{-1}, (XY)Z=X(YZ)$ für
  $X,Y,Z\subseteq G$. Der \autoref{satz:24} besagt: $X\leq G\Leftrightarrow
  X\neq\emptyset\wedge XX^{-1}\subseteq X$.
\end{bemerk}

\begin{satz}
  Für Untergruppen $U,V,W$ einer Gruppe $G$ gilt stets:
  \begin{enumerate}
   \item $U\cup V\leq G\Leftrightarrow U\subseteq V\vee V\subseteq U$
   \item $UV\leq G\Leftrightarrow UV=VU$
   \item $U\subseteq W\Rightarrow UV\cap W=U(V\cap W)$
    (\highl[Identität!\textsc{Dedekind}sche]{\textsc{Dedekind}sche
    Identität})
  \end{enumerate}
  \begin{proof}
    \begin{enumerate}
     \item "`$\Rightarrow$"': Sei $U\cup V\leq G$ und $U\nsubseteq V$. Dann
      gibt es ein Element $u\in U\setminus V$. Für $v\in V$ ist $uv\in U\cup
      V$. Im Fall $uv\in V$ wäre $u=uvv^{-1}\in V$ \lightning{} Also muss
      $uv\in U$ sein und $v=u^{-1}uv\in U$. Daher ist $V\subseteq U$.\\
      "`$\Leftarrow$"' ist trivial.
     \item $UV\leq G\Rightarrow(UV)=(UV)^{-1}= V^{-1}U^{-1}=VU$ und für
      $UV=VU$ folgt: $(UV)(UV)^{-1}= UVV^{-1}U^{-1}\subseteq UVU=UUV=UV$.
      Somit ist $UV$ eine Untergruppe.
     \item Sei $U\subseteq W$ und $w\in UV\cap W$. Wir schreiben $w=uv$ mit
      $u\in U$ und $v\in V$. Dann ist $v=u^{-1}w\in W$, d.\,h. $w=uv\in
      U(V\cap W)$. Umgekehrt ist $U(V\cap W) \subseteq UV$ $U(V\cap
      W)\subseteq WW\subseteq W$, d.\,h. $U(V\cap W)\subseteq UV\cap W$.
    \end{enumerate}
  \end{proof}
\end{satz}

%def 2.6
\begin{defini}[erzeugte Untergruppe]\label{def:26}
  Für jede nichtleere Familie $(U_{i})_{i\in I}$ von Untergruppen $U_{i}$
  einer Gruppe $G$ gilt stets: $\bigcap_{i\in I} U_{i}\leq G$. Insbesondere
  ist für $X\subseteq G$ der Durchschnitt $D$ aller Untergruppen $U\leq G$ mit
  $X\subseteq U$ eine Untergruppe von $G$. Man nennt $D=\colon\langle
  X\rangle$ die von $X$ \highl[Untergruppe!erzeugte]{erzeugte Untergruppe} von
  $G$. Für $X=\{a_{1},\dotsc, a_{n}\}$ schreibt man $\langle a_{1},\dotsc,
  a_{n}\rangle$.
\end{defini}

\begin{satz}
  Sei $G$ eine Gruppe und $X\subseteq G$ Dann besteht $\langle X\rangle$ aus
  den Elementen der Form $x_{1}^{\epsilon_{1}}\dotso x_{n}^{\epsilon_{n}}$ mit
  $n\in\N_{0}, x_{1},\dotsc, x_{n}\in X,
  \epsilon_{1},\dotsc,\epsilon_{n}\in\{\pm 1\}$. Im Fall $n=0$ interpretiert
  man das Produkt als $1$.
  \begin{proof}
    Die Menge $A$ der angegebenen Elemente ist eine Untergruppe von $G$, die
    $X$ enthält. Nach \autoref{def:26} ist also $\langle X\rangle\subseteq A$.
    Ist umgekehrt $U$ eine Untergruppe von $G$, die $X$ enthält, so enthält
    $U$ auch $A$. Nach \autoref{def:26} ist $A\subseteq\langle X\rangle$.
  \end{proof}
\end{satz}



\clearpage
\appendix
\begin{thebibliography}{99}
 \bibitem{alperin} \textsc{Alperin"=Bell}. Groups and representations.
 \bibitem{asch} \textsc{Aschbacher}. Finite group theory.
 \bibitem{issacs} \textsc{Isaacs}. Finite group theory.
 \bibitem{kurzweil} \textsc{Kurzweil-Stellmacher}. Theorie der endlichen Gruppen.
 \bibitem{robinson} \textsc{Robinson}.  course in the theory of groups.
 \bibitem{rotman} \textsc{Rotman}. An introduction to the theory of groups.
 \bibitem{huppert} \textsc{Huppert}. Endliche Gruppen I.
 \bibitem{blackburn} \textsc{Huppert"=Blackburn}. Finite groups II--III.
 \bibitem{suzuki} \textsc{Suzuki}. Group theory I--II.
 \bibitem{gorenstein} \textsc{Gorenstein}. Finite Groups.
 \bibitem{ronan} \textsc{Ronan}. Symmetry and the monster.
\end{thebibliography}

\clearpage
\pdfbookmark[0]{Index}{index}
\printindex

\end{document}
